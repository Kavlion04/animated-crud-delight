
export interface Contact {
  id: string;
  name: string;
  jobTitle: string;
  department: string;
  officePhone?: string;
  mobile?: string;
  email: string;
  avatarUrl?: string; // URL to an image
  linkedin?: string;
  twitter?: string;
  github?: string;
}
